export const TIMEOUTS = {
  IMAGE_GENERATION: 90000,
  MODEL_DOWNLOAD: 3600000,
  API_CALL: 30000,
  FILE_OPERATION: 10000,
  DEPENDENCY_CHECK: 15000,
  PYTHON_SUBPROCESS: 120000,
} as const;

export type TimeoutKey = keyof typeof TIMEOUTS;
